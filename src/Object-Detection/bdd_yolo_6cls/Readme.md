Training structure. No images because the dataset was too hughe but it can be download from a public repository BDD100K is the data set.
For our training we created a subset based on the original 100K images, to ahve 7000 training , 1000 validation, and 2000 test images.
The subset is balanced, including fair enough images for each class (Cars, Buses, Trucks, Motorcycles, Bicycles, and Pedestrians).
the data.yaml file is included as long as the labels.
These labels were converte from JSON to YOLO format.
